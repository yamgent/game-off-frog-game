using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AutoMove : MonoBehaviour
{
    // Start is called before the first frame update
    void Start() {
        
    }

    // Update is called once per frame
    void Update() {
        // Move upwards whenever it is near or within the camera bounds.

        // Speed is relative to current camera speed.

        // Die when it is lagging too far from camera.
    }
}
